import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.01counterapp',
  appName: '01counterapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
